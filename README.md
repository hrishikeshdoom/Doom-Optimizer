# Doom-Optimizer
Native Windows desktop app that applies real system-level optimizations — registry tweaks, custom power plans, GPU tuning, DNS switching, and RAM flush. Packages into a setup.exe installer. Every tweak is reversible with a one-click restore point backup.
